"use client";
import Accordion from "@/components/Accordion";
import React from "react";

const CreateScheduleItem = ({ title }: { title: string }) => {
  return <div>{title}</div>;
};

export default CreateScheduleItem;
